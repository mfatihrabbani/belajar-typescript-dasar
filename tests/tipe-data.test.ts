describe("Data Type", () => {
    it("should must declare", () => {
        const name : String = "Nama"
        const balance : number = 10000
        const isVip : boolean = true

        console.log(name)
        console.log(balance)
        console.log(isVip)
    })
})